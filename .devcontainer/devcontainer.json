// .devcontainer/devcontainer.json
{
  "name": "Next Nest Todo",
  "workspaceFolder": "/workspaces/next_nest_todo",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y redis-server",
  "forwardPorts": [3000, 4000, 6379]
}
